# Compatibilidad: este archivo se conserva para soportar
# `pip install -r requirements.txt`. La fuente de verdad de las
# dependencias es ``pyproject.toml``.
#
# Para instalar el paquete completo (runtime + dev + notebooks):
#     pip install -e ".[all]"
# Solo runtime:
#     pip install -e .
# Runtime + tests:
#     pip install -e ".[dev]"

# ── Runtime ──────────────────────────────────────────────────────────
pandas>=2.1,<3.0
numpy>=1.26,<2.0
requests>=2.31,<3.0
openpyxl>=3.1,<4.0
xlrd>=2.0,<3.0
beautifulsoup4>=4.12,<5.0
lxml>=5.0,<6.0
pdfplumber>=0.11,<1.0
scipy>=1.11,<2.0
statsmodels>=0.14,<1.0
matplotlib>=3.8,<4.0

# ── Dev ──────────────────────────────────────────────────────────────
pytest>=8.0,<9.0

# ── Notebooks ────────────────────────────────────────────────────────
jupyter>=1.0
